import { RoutineFilterPipe } from './routine-filter.pipe';

describe('RoutineFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new RoutineFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
